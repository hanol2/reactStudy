import ReactDOM from 'react-dom/client'; // 리액트와 리액트 돔 =>  두 의존성이 함께 리액트 라이브러리

import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
